PORT=5000
MONGO_URI=your_mongodb_uri
JWT_SECRET=your_secret_key
GOOGLE_CLIENT_ID=your_google_id
GOOGLE_CLIENT_SECRET=your_google_secret
FRONTEND_URL=http://localhost:5173

# Email (via Vercel Email Proxy - SMTP vars go in Vercel dashboard, not here)
# Emails are proxied through the frontend at FRONTEND_URL/api/send-email.
# For local development without the proxy, emails are logged to console.
